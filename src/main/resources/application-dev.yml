spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://127.0.0.1:3306/employee_management}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:}
  jpa:
    hibernate:
      ddl-auto: update

logging:
  level:
    com.example.employeemanagement: DEBUG

app:
  security:
    jwt:
      secret: ${JWT_SECRET:bGFiLTktZGV2LW9ubHktc2VjcmV0LWtleS1jaGFuZ2UtbWUtMjAyNg==}
    admin:
      username: ${ADMIN_USERNAME:admin}
      password: ${ADMIN_PASSWORD:admin12345}
